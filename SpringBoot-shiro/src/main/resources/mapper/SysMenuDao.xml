<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.onlytype.dao.SysMenuDao">

    <resultMap type="xyz.onlytype.entity.SysMenu" id="SysMenuMap">
        <result property="menuId" column="menu_id" jdbcType="INTEGER"/>
        <result property="menuName" column="menu_name" jdbcType="VARCHAR"/>
        <result property="menuUrl" column="menu_url" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="menuId" useGeneratedKeys="true">
        insert into minio.sys_menu(menu_name, menu_url, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.menuName}, #{entity.menuUrl}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="menuId" useGeneratedKeys="true">
        insert into minio.sys_menu(menu_name, menu_url, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.menuName}, #{entity.menuUrl}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
        on duplicate key update
        menu_name = values(menu_name) , menu_url = values(menu_url) , create_time = values(create_time) , update_time = values(update_time)     </insert>

</mapper>

